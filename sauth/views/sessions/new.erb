<!DOCTYPE html>
<html>
<head>
  <title>login page</title>
</head>
<body>

<h1>Identification</h1>

<% if @error_informations %>
	<p class="error_information">Une erreur est survenue lors de la saisie des informations.</p>
<% end %>

<% if @error_login_not_exists %>
	<p class="error_login_not_exists">Le login n'existe pas. Etes vous sûr d'avoir un compte?</p>
	<a href="/users/new?origin=<%= params['origin'] %>&nameapp=<%= params['app_name'] %>">Créer un compte</a>
<% end %>
	
<form action="<%= '/' + params['app_name'] unless params['app_name'].nil? %>/sessions" method="post">
  <div class="field">
    <label for="session_login">Login</label><br />

    <input id="session_login" name="login" size="30" type="text" />
  </div>
  <div class="field">
    <label for="session_password">Password</label><br />
    <input id="session_password" name="password" size="30" type="password" />
  </div>
  <input type="hidden" name="origin" value="<%= params['origin'] %>" />
  <div class="actions">
    <input name="commit" type="submit" value="M'identifier" />

  </div>
</form>

<a href="/users/new?origin=<%= params['origin'] %>&nameapp=<%= params['app_name'] %>">Créer un nouveau compte</a><br/>
</body>
</html>
