<!DOCTYPE html>
<html>
<head>
  <title>Page d'administration</title>
</head>
<body>
	<p>Bonjour <%= @user %> </p>

	<% if !@error_admin %>
		<p> Vous avez tous les droits du service d'authentification </p>

		<div class="list_user">
			 <% if @user.length > 1 %>
             	<p>Voici les utilisateurs du service d'authentification :</p>
            	<ul>
              		<% @list_user.each do |u| %> 
                		<%if u.login != "admin"%> 
                  			<li><%=u.login%> - <a href="/sauth/users/delete?user=<%=u.id%>">Supprimer cet utilisateur</a> </li>
                		<% end %>              
              		<% end %>
            	</ul>
			<% else %>
				<p> Aucun utilisateurs ne sont enregistrés sur votre service d'authentification <p>
			<% end %>
			<% if @apps %>
					<div class="applications">
						<p>Voici la liste d'applications dont vous êtes l'administrateur</p>
						<ul>
							<% @apps.each do |a| %>
								<li><a href="<%=a.url%>"> <%=a.name%></a> <a href="/app/delete?app=<%=a.id%>">Supprimer</a> </li>
							<% end %>
						</ul>
					</div>
			<%else%>
				<p>Vous êtes administrateur d'aucune application</p>
			<%end%>

	 <% if @util %>
		<div class="Utilisations">
			<p>Voici la liste d'applications que vous utilisez</p>
			<ul>
				<% @util.each do |u| %>
					<% app = App.find_by_id(u.app_id) %>
					<li><a href="<%=app.url%>"><%=app.name%></a></li>
				<% end %>
			</ul>
		</div>

	<%else%>
		<p>Vous n'utilisez aucune application</p>
	<%end%>
	<% else %>
		<p> Vous n'avez aucun droits car vous n'êtes pas administrateur du service d'authentification </p>
	<% end %>

<a href="/app/new">Créer une nouvelle application</a><br/>
<a href="/sessions/disconnect">Se déconnecter</a>

</body>
</html>
